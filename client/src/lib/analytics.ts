// Google Analytics integration - Placeholder
// GA tracking has been replaced with GA4 Data API for viewing analytics in the dashboard
// The actual tracking happens on the main psychicsource.com website

export const initGA = () => {
  // GA tracking is handled by the main website, not this CMS
};

export const trackPageView = (url: string) => {
  // GA tracking is handled by the main website, not this CMS
};

export const trackEvent = (
  action: string, 
  category?: string, 
  label?: string, 
  value?: number
) => {
  // GA tracking is handled by the main website, not this CMS
};
